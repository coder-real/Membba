node_modules
dist
.env
.env.local
.DS_Store

# WhatsApp / Baileys session — local only, never commit
.wwebjs_auth/
.wwebjs_cache/
baileys_auth/

# Zip archives — don't commit these
*.zip

.cache
.cache/
